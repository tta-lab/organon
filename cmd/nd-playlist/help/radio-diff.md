Compare a radio YAML spec with Navidrome without changing the server.

  nd-playlist radio diff playlists/navidrome/radios/cliamp.yaml

Stations match by `stream_url`. Output marks each entry as `exists` or `would add`.
