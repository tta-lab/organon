Search Navidrome songs with `search3.view`.

Examples:
  nd-playlist search "小半 陈粒"
  nd-playlist search --json "Everything's Alright Kan Gao"

Use this to inspect candidate song IDs before writing a playlist spec.
