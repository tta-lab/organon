Get a project by alias. Falls back to reference repos if not found in projects.toml.

  project get <alias>          # print filesystem path
  project get <alias> --json   # JSON with alias, name, path, org
