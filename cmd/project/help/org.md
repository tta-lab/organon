List and get orgs from orgs.toml.

  project org list              # list all orgs
  project org list --json       # JSON output
  project org get <name>        # show a single org
  project org get <name> --json # JSON output
