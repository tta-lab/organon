Find active registered projects and locally cloned reference repositories by
relevance. Registered projects shadow same-named references.

  og project find <query>...
  og project find <query>... --limit 16
  og project find <query>... --json
