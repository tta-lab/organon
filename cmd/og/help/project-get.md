Get a project by an exact case-insensitive alias, checkout basename, or remote
repository basename. Human output retains explicit local reference-repository
fallback; JSON is registered-project data only.

  og project get <project-reference>
  og project get <project-reference> --json
