# og

Organon forge operations.

`og` is the local entrypoint for typed repository and forge workflows. V1
contains command skeletons for pull request, git, auth, policy, and daemon
operations. Merge is intentionally out of scope.

