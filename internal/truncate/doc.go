// Package truncate applies the Pi tool-output truncation contract to CLI JSON
// results: a 2,000-line and 50-KB pair of independent limits, whichever is hit
// first, never returning partial lines.
//
// Plane: shared
package truncate
